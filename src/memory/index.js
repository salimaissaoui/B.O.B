/**
 * Memory Module Index
 *
 * Central entry point for memory and learning functionality.
 */

import BuildMemory, { getMemory } from './build-memory.js';

export { BuildMemory, getMemory };

export default { BuildMemory, getMemory };
