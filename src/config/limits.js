export const SAFETY_LIMITS = {
  maxBlocks: 10000,
  maxUniqueBlocks: 15,
  maxHeight: 256,
  maxWidth: 100,
  maxDepth: 100,
  buildRateLimit: 10,
  maxRetries: 3,
  dayDuration: 600,  // seconds (30 in-game minutes)
  nightDuration: 90
};
