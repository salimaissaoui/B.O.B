export const SAFETY_LIMITS = {
  maxBlocks: 10000,
  maxUniqueBlocks: 15,
  maxHeight: 256,
  maxWidth: 100,
  maxDepth: 100,
  maxSteps: 1000,
  buildRateLimit: 50,
  maxRetries: 3,
  llmTimeoutMs: 120000,
  llmMaxRetries: 3,
  llmRetryDelayMs: 750,
  dayDuration: 600,  // seconds (30 in-game minutes)
  nightDuration: 90
};
